import React from 'react';

import classes from './MainLoader.module.css';

const MainLoader = (props) => {

    return (
        <div className={classes.MainLoader}>
            <div className={classes.MainLoader__loader}>
            
            </div>
        </div>
    );
};

export default MainLoader;